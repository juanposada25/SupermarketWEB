@page
@model SupermarketWEB.Pages.PayModes.IndexModel
@{
    ViewData["Title"] = "Index PayModes";
}
<h1 class="text-center my-4">Index PayModes</h1>

<p class="text-center">
    <a asp-page="Create" class="btn btn-success btn-lg mb-3">Create New PayMode</a>
</p>

<div class="table-responsive">
    <table class="table table-bordered table-hover table-striped">
        <thead class="table-dark">
            <tr>
                <th class="text-center">@Html.DisplayNameFor(model => model.PayModes[0].Name)</th>
                <th class="text-center">@Html.DisplayNameFor(model => model.PayModes[0].Observation)</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.PayModes)
            {
                <tr>
                    <td class="text-center align-middle">@Html.DisplayFor(modelItem => item.Name)</td>
                    <td class="text-center align-middle">@Html.DisplayFor(modelItem => item.Observation)</td>
                    <td class="text-center align-middle">
                        <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-warning mx-1">Edit</a>
                        <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-danger mx-1">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    body {
        background-color: #f0f8ff;
    }
    h1 {
        color: #333333;
    }
    .table thead th {
        color: #ffffff;
    }
    .table tbody tr:hover {
        background-color: #e0f7fa;
    }
    .btn {
        font-weight: bold;
    }
</style>
